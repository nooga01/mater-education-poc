{% macro render(item) %}

<article class="course-detail">
   {% if item.title %}
      <h2>{{ item.title }}</h2>
   {% endif %}      
   {% if item.body %}
      <div>
         {{ item.body | safe }}
      </div>
   {% endif %}
</article>

{% endmacro %}
