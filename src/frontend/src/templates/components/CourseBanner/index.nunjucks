{% macro render(item) %}
   <div class="course-banner">
         {% if item.course_title %}
            <h1 class="hero-title">{{ item.course_title }}</h1>
          {% endif %}
          {% if item.course_code %}
            <h4>{{ item.course_code }}</h4>
         {% endif %}
   </div>

{% endmacro %}
